<%= turbo_frame_tag "cart" do %>
    <h1>Cart</h1>
    <% if @cart.items.count > 0 %>
        <%= render "cart/items" %>
        <p>Total: <%= @cart.total%> </p>
        <% else %>
        <p>Your cart is empty</p>
    <% end %>
<% end %>